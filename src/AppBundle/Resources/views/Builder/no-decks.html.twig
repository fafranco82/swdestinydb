{% extends 'AppBundle::layout.html.twig' %}

{% block body %}

<div class="jumbotron" id="no-deck-available">
  <div class="main white container">
    {% include 'AppBundle:Builder:no-decks-text.'~ app.request.locale ~'.html.twig' %}
  </div>
</div>
<div style="text-align:center">
		<a href="{{ path('deck_buildform') }}" class="btn btn-default btn-lg"><span class="fa fa-plus"></span> {{ 'decks.form.build' | trans }}</a>
		<a href="{{ path('deck_import') }}" class="btn btn-default btn-lg"><span class="fa fa-upload"></span> {{ 'forms.import' | trans }}</a>
		<button type="button" class="btn btn-default btn-lg" onclick="$('#fileModal').modal('show')"><span class="fa fa-upload"></span> {{ 'forms.upload' | trans}}</button>
		<button type="button" class="btn btn-default btn-lg" onclick="$('#archiveModal').modal('show')"><span class="fa fa-upload"></span> {{ 'decks.form.uploadarchive' | trans}}</a>
</div>

{% include 'AppBundle:Modale:file.html.twig' %}
{% include 'AppBundle:Modale:archive.html.twig' %}

{% endblock %}
